= link_to 'Back', :back, class: 'btn btn-default'
- if can? :manage, @property
  = link_to 'Delete', @property, class: 'btn btn-danger', confirm: 'Are you sure?', method: :delete

h1 Editing '#{@property.name}'

= render 'form'
