= form_tag search_path, method: 'get' do
  = text_field_tag :q
  = submit_tag 'Search'

- if @formula
  = @formula

- if @results
  h3 Results
  = will_paginate @results, renderer: BootstrapPagination::Rails
  ul
    - @results.each do |s|
      li= s