h1
  = @space.name 
  |  
  = link_to "View", space_path(@space), class: 'btn btn-mini'
#proofs_pane.well
  h1.loading Loading ...
  #info_pane

= onload do
  | new pi_baseProofExplorer().render("#proofs_pane");
