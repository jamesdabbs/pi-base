h1= @space.name
p= Markdown.render(@space.description).html_safe

h3 Traits
= will_paginate @traits, renderer: BootstrapPagination::Rails
ul
  - @traits.each do |trait|
    li= link_to "#{trait.property}: #{trait.value}", trait