require 'spec_helper'

describe Property do
  # let(:p1) { Property.new }
  # let(:p2) { Property.new }

  it "can be add'd to form a conjunction"
    # expect(p1 + p2).to be_a_kind_of Formula::Conjunction
  end

  it "can be or'd to form a disjunction"
    # expect(p1 | p2).to be_a_kind_of Formula::Disjunction
  end
end